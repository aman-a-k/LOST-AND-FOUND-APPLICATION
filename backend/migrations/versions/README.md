# Database Migrations

This directory contains database migration scripts generated by Flask-Migrate.

When you make changes to your database models, run:
```bash
flask db migrate -m "Description of changes"
```

To apply the migrations to your database, run:
```bash
flask db upgrade
```

To revert migrations, run:
```bash
flask db downgrade
```